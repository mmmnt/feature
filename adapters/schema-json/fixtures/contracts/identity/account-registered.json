{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://mmmnt.dev/fixtures/identity/account-registered.json",
  "allOf": [{ "$ref": "../adrs/event-envelope.json" }],
  "type": "object",
  "required": ["accountId"],
  "properties": {
    "accountId": { "$ref": "#/$defs/accountId" },
    "actor": { "$ref": "../adrs/event-envelope.json#/$defs/uuid" }
  },
  "$defs": {
    "accountId": { "type": "string", "pattern": "^acct_" }
  }
}
